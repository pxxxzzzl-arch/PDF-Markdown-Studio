services:
  pdfmd:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - pdfmd-data:/data
    environment:
      PDFMD_MAX_WORKERS: "1"
      PDFMD_MAX_PAGES: "500"
    restart: unless-stopped

volumes:
  pdfmd-data:
